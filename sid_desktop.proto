syntax = "proto3";

// run `protoc --go_out=plugins=grpc:. ./sid_desktop.proto` in root directory

option go_package = "github.com/BabySid/proto";

package desktop;

//service SidDesktop {
//  rpc UploadWebInfo(UploadWebInfoRequest) returns (UploadWebInfoResponse) {}
//}

message ScriptRunner {
  int32 id = 1;
  string title = 2;
  string content = 3;
}

